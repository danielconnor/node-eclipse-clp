
scattered(++PredSpec)

   This declaration is obsolete, use discontiguous/1 instead.

Arguments
   PredSpec            Structure of the form Name/Arity, or a comma-separated sequence of those

Type
   library(scattered)

See Also
   discontiguous / 1
